ent-RandomBedraggledTreeSpawner = random bedraggled tree spawner
    .desc = Places a random bedraggled tree variant when the map loads.

ent-RandomDeadTreeSpawner = random dead tree spawner
    .desc = Places a random dead tree variant when the map loads.

ent-RandomTreeStumpSpawner = random tree stump spawner
    .desc = Places a random tree stump variant when the map loads.

ent-RandomGreatBushSpawner = random great bush spawner
    .desc = Places a random great bush variant when the map loads.

ent-RandomFallenLogSpawner = random fallen log spawner
    .desc = Places a random fallen log variant when the map loads.

ent-RandomGiantMushroomSpawner = random giant mushroom spawner
    .desc = Places a random giant mushroom variant when the map loads.

ent-RandomGravemarkerSpawner = random grave marker spawner
    .desc = Places a random grave marker variant when the map loads.

ent-RandomPsyGravemarkerSpawner = random Psy grave marker spawner
    .desc = Places a random Psy grave marker variant when the map loads.

ent-RandomGravestoneSpawner = random gravestone spawner
    .desc = Places a random gravestone variant when the map loads.

ent-RandomHangingChainSpawner = random hanging chain spawner
    .desc = Places a random hanging chain variant when the map loads.

ent-RandomRusticChairSpawner = random rustic wooden chair spawner
    .desc = Places a random rustic wooden chair variant when the map loads.

ent-RandomKnightStatueSpawner = random knight statue spawner
    .desc = Places a random knight statue variant when the map loads.

ent-RandomAnimalRugSpawner = random animal rug spawner
    .desc = Places a random animal rug variant when the map loads.

ent-RandomWoodenTableSpawner = random wooden table spawner
    .desc = Places a random wooden table variant when the map loads.

ent-RandomMapTableSpawner = random map table spawner
    .desc = Places a random map table variant when the map loads.

ent-RandomWildHerbSpawner = random wild herb spawner
    .desc = Places a random wild herb variant when the map loads.
